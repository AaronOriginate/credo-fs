<!-- Section 22 — FAQ Accordion | Figma: fileKey=RxueMLDFnpgheWZp4MVk7f nodeId=4:929 -->
<section data-figma-ref="4:929" class="bg-gradient-to-b from-light-gray to-white py-16 sm:py-24 lg:py-28">
  <div class="max-w-[1232px] mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Label + divider -->
    <p class="text-xs uppercase tracking-widest font-medium text-dark text-right mb-4">FAQ</p>
    <div class="w-full h-px bg-accent mb-12"></div>
    <!-- Heading -->
    <h2 class="font-heading font-medium text-dark text-center text-3xl sm:text-4xl lg:text-[56px] lg:leading-[1.25] tracking-tight mb-12">
      Questions I get asked a lot
    </h2>
    <!-- FAQ card -->
    <div class="bg-navy rounded-lg px-8 sm:px-16 py-12" id="faq-accordion">
      <!-- FAQ 1 (open) -->
      <div class="faq-item border-b border-accent" data-open="true">
        <button class="faq-trigger w-full text-left py-6 flex items-center justify-between gap-4 bg-transparent border-0 cursor-pointer" aria-expanded="true">
          <span class="font-heading font-medium text-white text-lg sm:text-2xl">Is the initial review really free?</span>
          <svg class="faq-icon w-4 h-4 shrink-0 text-white transition-transform rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/></svg>
        </button>
        <div class="faq-content pb-6">
          <p class="text-white text-base sm:text-lg leading-relaxed max-w-2xl">
            Yes. I'll go through your statements, check your rates, and tell you straight. If you're already on a good deal, that's the end of it. Costs you nothing.
          </p>
        </div>
      </div>
      <!-- FAQ 2 -->
      <div class="faq-item border-b border-accent" data-open="false">
        <button class="faq-trigger w-full text-left py-6 flex items-center justify-between gap-4 bg-transparent border-0 cursor-pointer" aria-expanded="false">
          <span class="font-heading font-medium text-white text-lg sm:text-2xl">How do you make money if the review is free?</span>
          <svg class="faq-icon w-4 h-4 shrink-0 text-white transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/></svg>
        </button>
        <div class="faq-content hidden pb-6">
          <p class="text-white text-base sm:text-lg leading-relaxed max-w-2xl">
            If I find a better deal and you decide to switch, I earn a small commission from the provider. But I only recommend a change if it genuinely saves you money. That's the whole point of being independent.
          </p>
        </div>
      </div>
      <!-- FAQ 3 -->
      <div class="faq-item border-b border-accent" data-open="false">
        <button class="faq-trigger w-full text-left py-6 flex items-center justify-between gap-4 bg-transparent border-0 cursor-pointer" aria-expanded="false">
          <span class="font-heading font-medium text-white text-lg sm:text-2xl">How long does switching take?</span>
          <svg class="faq-icon w-4 h-4 shrink-0 text-white transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/></svg>
        </button>
        <div class="faq-content hidden pb-6">
          <p class="text-white text-base sm:text-lg leading-relaxed max-w-2xl">
            Usually a couple of weeks from start to finish. I handle the setup, make sure everything works, and I'm there on the day to make sure the changeover goes smoothly.
          </p>
        </div>
      </div>
      <!-- FAQ 4 -->
      <div class="faq-item border-b border-accent" data-open="false">
        <button class="faq-trigger w-full text-left py-6 flex items-center justify-between gap-4 bg-transparent border-0 cursor-pointer" aria-expanded="false">
          <span class="font-heading font-medium text-white text-lg sm:text-2xl">Will I be locked into a long contract?</span>
          <svg class="faq-icon w-4 h-4 shrink-0 text-white transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/></svg>
        </button>
        <div class="faq-content hidden pb-6">
          <p class="text-white text-base sm:text-lg leading-relaxed max-w-2xl">
            I don't recommend providers who lock you in. If a deal requires a long commitment, I'll tell you upfront and explain why. Most of the setups I recommend are rolling or short-term.
          </p>
        </div>
      </div>
      <!-- FAQ 5 -->
      <div class="faq-item border-b border-accent" data-open="false">
        <button class="faq-trigger w-full text-left py-6 flex items-center justify-between gap-4 bg-transparent border-0 cursor-pointer" aria-expanded="false">
          <span class="font-heading font-medium text-white text-lg sm:text-2xl">What happens if something goes wrong after setup?</span>
          <svg class="faq-icon w-4 h-4 shrink-0 text-white transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/></svg>
        </button>
        <div class="faq-content hidden pb-6">
          <p class="text-white text-base sm:text-lg leading-relaxed max-w-2xl">
            You call me. My mobile. That's how it works with every client I have.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  const faqAccordion = document.getElementById("faq-accordion");
  if (faqAccordion) {
    const triggers = faqAccordion.querySelectorAll(".faq-trigger");
    triggers.forEach((trigger) => {
      trigger.addEventListener("click", () => {
        const item = trigger.closest(".faq-item");
        if (!item) return;
        const isOpen = item.getAttribute("data-open") === "true";

        // Close all
        faqAccordion.querySelectorAll(".faq-item").forEach((el) => {
          el.setAttribute("data-open", "false");
          const content = el.querySelector(".faq-content");
          const icon = el.querySelector(".faq-icon");
          const btn = el.querySelector(".faq-trigger");
          if (content) content.classList.add("hidden");
          if (icon) icon.classList.remove("rotate-180");
          if (btn) btn.setAttribute("aria-expanded", "false");
        });

        // Open clicked (if was closed)
        if (!isOpen) {
          item.setAttribute("data-open", "true");
          const content = item.querySelector(".faq-content");
          const icon = item.querySelector(".faq-icon");
          if (content) content.classList.remove("hidden");
          if (icon) icon.classList.add("rotate-180");
          trigger.setAttribute("aria-expanded", "true");
        }
      });
    });
  }
</script>
