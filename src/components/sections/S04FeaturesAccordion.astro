<!-- Section 04 — Features Accordion | Figma: fileKey=RxueMLDFnpgheWZp4MVk7f nodeId=8:107 -->
<section data-figma-ref="8:107" class="bg-white py-20 sm:py-28 lg:py-32">
  <div class="max-w-[1232px] mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section label -->
    <p class="text-xs uppercase tracking-widest text-gray-text mb-4">What I see every day</p>
    <div class="w-12 h-0.5 bg-accent mb-12 sm:mb-16"></div>

    <!-- 2-col grid -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20">
      <!-- Left: heading + accordion -->
      <div>
        <h2 class="font-heading font-medium text-dark text-3xl sm:text-4xl lg:text-5xl leading-tight mb-6">
          Are you bombarded by sales calls?
        </h2>
        <p class="text-gray-text mb-10">
          Every week, another rep calls promising the best deal. How would you know? You're busy running a business. Here's what I keep finding when I actually look at the numbers.
        </p>

        <!-- Accordion -->
        <div class="flex flex-col" id="accordion">
          <!-- Item 1 (open by default) -->
          <div class="accordion-item border-b border-accent" data-open="true">
            <button
              class="accordion-trigger w-full text-left py-5 flex items-center justify-between gap-4 bg-transparent border-0 cursor-pointer"
              aria-expanded="true"
            >
              <span class="font-heading font-medium text-dark text-lg">
                She'd been overpaying for three years
              </span>
              <svg class="accordion-icon w-5 h-5 shrink-0 text-dark transition-transform rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
              </svg>
            </button>
            <div class="accordion-content pb-6">
              <p class="text-gray-text text-sm leading-relaxed">
                A restaurant owner in Bristol was paying 2.9% on every transaction. For a business turning over &pound;1.4M in cards, that's &pound;12,000+ a year in fees. I moved her to 1.1%. Same machine. Just the right deal.
              </p>
            </div>
          </div>

          <!-- Item 2 -->
          <div class="accordion-item border-b border-accent" data-open="false">
            <button
              class="accordion-trigger w-full text-left py-5 flex items-center justify-between gap-4 bg-transparent border-0 cursor-pointer"
              aria-expanded="false"
            >
              <span class="font-heading font-medium text-dark text-lg">
                The providers know this
              </span>
              <svg class="accordion-icon w-5 h-5 shrink-0 text-dark transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
              </svg>
            </button>
            <div class="accordion-content hidden pb-6">
              <p class="text-gray-text text-sm leading-relaxed">
                Providers rely on the fact that most business owners are too busy to compare rates or read the fine print. They count on inertia &mdash; because switching sounds complicated. It isn't, once someone shows you how.
              </p>
            </div>
          </div>

          <!-- Item 3 -->
          <div class="accordion-item border-b border-accent" data-open="false">
            <button
              class="accordion-trigger w-full text-left py-5 flex items-center justify-between gap-4 bg-transparent border-0 cursor-pointer"
              aria-expanded="false"
            >
              <span class="font-heading font-medium text-dark text-lg">
                Their whole model depends on you not checking
              </span>
              <svg class="accordion-icon w-5 h-5 shrink-0 text-dark transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
              </svg>
            </button>
            <div class="accordion-content hidden pb-6">
              <p class="text-gray-text text-sm leading-relaxed">
                Hidden fees, confusing statements, rates that creep up after the first year. If no one's looking, no one's complaining. That's the business model. I look. And I find it every time.
              </p>
            </div>
          </div>
        </div>
      </div>

      <!-- Right: placeholder image -->
      <div class="flex items-center">
        <div class="w-full aspect-[4/3] bg-navy rounded-2xl"></div>
      </div>
    </div>
  </div>
</section>

<script>
  const accordion = document.getElementById("accordion");
  if (accordion) {
    const triggers = accordion.querySelectorAll(".accordion-trigger");
    triggers.forEach((trigger) => {
      trigger.addEventListener("click", () => {
        const item = trigger.closest(".accordion-item");
        if (!item) return;
        const isOpen = item.getAttribute("data-open") === "true";

        // Close all
        accordion.querySelectorAll(".accordion-item").forEach((el) => {
          el.setAttribute("data-open", "false");
          const content = el.querySelector(".accordion-content");
          const icon = el.querySelector(".accordion-icon");
          const btn = el.querySelector(".accordion-trigger");
          if (content) content.classList.add("hidden");
          if (icon) icon.classList.remove("rotate-180");
          if (btn) btn.setAttribute("aria-expanded", "false");
        });

        // Open clicked (if it was closed)
        if (!isOpen) {
          item.setAttribute("data-open", "true");
          const content = item.querySelector(".accordion-content");
          const icon = item.querySelector(".accordion-icon");
          if (content) content.classList.remove("hidden");
          if (icon) icon.classList.add("rotate-180");
          trigger.setAttribute("aria-expanded", "true");
        }
      });
    });
  }
</script>
